import { Card, Text } from "@sanity/ui";

const GettingReadyCard = () => {
  return (
    <Card borderBottom as="li" padding={4} radius={2}>
      <Text as="em">Getting things ready...</Text>
    </Card>
  );
};

export default GettingReadyCard;
